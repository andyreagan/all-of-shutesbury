[project]
name = "all-of-shutesbury"
version = "0.1.0"
description = "Road inventory + GPX coverage checker for riding every road in Shutesbury, MA"
requires-python = ">=3.11"
dependencies = [
    "shapely>=2.0",
    "pyproj>=3.6",
    "networkx>=3.2",
]
